import types from '../../common/types';

export const admin=(state ={}, action)=>{
    let newState = state;
    switch(action.type){
        case types.successUploadMessages:
            break;
    }
    return newState;
};
